import 'package:flutter/material.dart';


class UserCalendaer extends StatelessWidget {
  const UserCalendaer({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Calender Page",
        style: TextStyle(fontSize: 50),
      ),
    );
  }
}